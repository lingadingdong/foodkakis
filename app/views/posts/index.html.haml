- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br/
		%span
			Share your lobang!
- else
	%p#intro
		Looking for foodkakis?
		%br/
		%span
			Join and share the love!
#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size